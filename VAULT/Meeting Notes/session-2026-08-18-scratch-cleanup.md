# Scratch-script cleanup — 2026-08-18

## Контекст

Расследование 2026-07-28...2026-08-18 показало паттерн: в `server/` копилось 81 неотслеживаемых диагностических скрипта (`check_*.js`, `test_*.js`, `find_*.js`, `deep_gps_research*.js`, `bdd_*.js`, `recheck_*.js`) — все от одной сессии-расследования 2026-08-12/13, ни один никогда не был закоммичен (`git log -- check_*.js` = 0).

Все проверенные (выборка + весь zikuy-кластер) — чисто read-only DAX/SQL запросы с `console.log`, ничего не пишут, друг от друга не зависят. Вывод (`console.log`) нигде не сохранялся — только вопрос остался в файле, ответ ушёл вместе с закрытым терминалом. Восстановить находки можно только сверкой с тем, что реально доехало до продакшена.

## Что подтверждено доехавшим до прода

- **zikuy (% זיכויים)** — кластер `check_zikuy_data/history/measure/measure_bug/measure_candidates/pct_per_sku/raw_closed_months.js`. Мера `% זיכויים` / `זיכויים` подтверждена используемой в `server/build-mmd-orders.js`, `server/index.js`, `docs/zikuy-order.html`. Расследование закрыто, вывод в коде, отдельного финдинга нет — только запросы, которыми к этому пришли.
- Остальные темы (chain-классификация клиента, kashrut-поля CUSTSPEC, shuk hair баг, wafer-семьи INTER, ICE BDD gap, 304201/304203 SKU naming, motagim, yishpro-клиент) — по каждому не сверял индивидуально с продом. Часть тем могла быть заброшена без вывода.

## Действие

Удалены 71 файл (все темы кроме GPS-кластера — см. ниже). Файлы никогда не были в git, восстановление не требуется.

## Оставлено — GPS-кластер (активная тема, не трогать)

`bdd_family_check.js`, `bdd_family_fix.js`, `bdd_gps_form.js`, `bdd_gps_research.js/2/3.js`, `deep_gps_research.js/2/3/4.js` — по прямому указанию пользователя, тема ещё активна.

## Правило вперёд

Диагностический скрипт закрывается в той же сессии, где создан:
- либо вывод фиксируется одной строкой (Vault/комментарий в проде) сразу после получения — не откладывать,
- либо файл удаляется сразу, если вопрос закрыт или заброшен.
Не переносить как долг на следующую сессию. Связано с фокус-режимом CEO (`current_focus`, `.claude/AGENTS/ceo-agent/AGENT.md`).

#status/done
