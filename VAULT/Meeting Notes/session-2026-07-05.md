# Session 2026-07-05 — MMD cache bust + Hebrew BiDi audit + Prophet

**Status:** #done #topic/mmd-orders #topic/hebrew-bidi #topic/prophet

## Выполнено (вечерняя сессия)

### MMD ORDERS — кэш [v07] для всех пользователей
- Добавлен редирект `GET /mmd/?r=1` → 302 → `/mmd/?r=2` в server/index.js (~строка 2422)
- Все пользователи с закладкой `r=1` (включая Eilat) автоматически получают свежий HTML
- Cloudflare видит новый URL `r=2` → cache miss → fresh response
- Подтверждено: `302 Location: /mmd/?r=2&k=test`

### Prophet — разбор +168% badge
- Формула: `(avg_last4 / avg_prev4 - 1) × 100`
- Badge появляется при `trendPct > 30%` — это рост за последние 4 недели vs предыдущие 4

### Аудит Hebrew/BiDi в кодовой базе
- Найдено 5 стратегий: fixBiDi (×4 копии), fixVisualRTL (×4), fixHebRTL (×5), fixBiDiAddress, _ltrWrap
- Код не трогали — только задокументировали
- Создан скилл: `.claude/SKILLS/hebrew-bidi/SKILL.md`
- Сохранено в memory: `project_hebrew_bidi_map`

## Пуш
- `fix(mmd): redirect r=1 -> r=2 to bust Cloudflare cache for all users`

---

# Session 2026-07-05 — Formula Road: OSRM trip, GPS sync, Excel overhaul

**Status:** #done

## Выполнено

### Bug fixes
- **confirmCity fix** — при смене города сбрасывает старые координаты + await геокодирования перед selectDay (маршрут теперь строится от правильного старта)
- **IS_LOCAL bug** — локальный cloudflared (PID 10984) + локальный сервер (PID 62012) убиты → пользователи видели IS_LOCAL заглушку вместо приложения
- **formula-road-data.json устарел** — CI Actions не деплоил файл на VPS → добавлен SCP шаг в formula-road-build.yml + секрет VPS_SSH_KEY в GitHub

### OSRM /trip road-optimal ordering
- Добавлен `OSRM_TRIP_CACHE` + функция `osrmTripOrder()`
- Гибридный подход: nearestNeighbor мгновенно → OSRM /trip async → обновление списка + тост "🛣️ סדר עודכן לפי כביש"
- Кэш по ключу start+clients(sorted by custId) — повторные открытия без запросов
- "🔄 חשב מחדש" очищает оба кэша (OSRM_CACHE + OSRM_TRIP_CACHE)

### Excel all-days (כל הימים)
- **dayOverrides** — клиент перенесённый 📅 в другой день появляется в правильном листе
- **savedOrders** — ручной drag-reorder порядка передаётся с клиента (localStorage fr_АГЕНТ_ДЕНЬ) и применяется на сервере

### GPS Pending Excel (одноразовый)
- Скачан локально: 34 pending corrections
- 3 листа: GPS Pending (синий), חסר GPS (красный), לא מדויק (оранжевый)

### Прочее (из предыдущего контекста)
- OSRM trip с кешем
- City chips redesign + client count
- OSM default tile
- GPS sync check endpoints
- CHANGED marker (haversine > 20m)
- [F-01] DAX company whitelist, [F-02] token out of URL, [F-04] auth on dagim-sales

## Коммиты
- `bec7d4ed` — feat(formula-road): OSRM trip ordering, multi-day xlsx, GPS sync, city chips, OSM default
- `37d7fee7` — feat(formula-road): GPS pending 3 sheets, all-days xlsx dayOverrides+savedOrders, CI VPS deploy
