# Session 2026-05-24 — Watchdog Fix + Dagim TRN + UI defaults

**Статус:** ✅ done

## Что было сделано

### 1. Диагностика и фикс FLOW / watchdog
- `watchdog.ps1` не запускался из-за синтаксической ошибки: em dash `—` в строках (UTF-8 байт `0x94` читался как `"` в Windows-1252 → ломал PowerShell parser)
- Фикс: заменили `—` на `--` во всех строках watchdog.ps1
- Дополнительно: `StartWhenAvailable = True` (не пропускать если комп спал), `DisallowStartIfOnBatteries = False`
- Фикс даты: `$today` использовал `dd/MM/yyyy` но лог пишет `MM/dd/yyyy` → исправлено
- GitHub Actions работает автономно (последний run: 23.05 в 23:13) — подтверждено через health-report.json
- CSV (`ברירת מחדל`) — локальные файлы, не в git. При выключенном компе GitHub Actions берёт последнюю версию base JSON из git

### 2. Дефолтный вид планограммы
- `hideReserve = true` → `false` — открывается без "Plan Only"
- Кнопка "Plan Only" стартует неактивной (без ✓)
- При переключении вкладок: `applyZoomToSection()` → `fitToScreen()` — каждая вкладка автоматически подгоняется по экрану

### 3. Кнопка מעבר (TRN) для דגים
- Добавлена кнопка `מעבר` (оранжевая) в строку складов
- Видна **только** на вкладке דגים, на других скрыта
- При уходе с דגים автоматически сбрасывается на Main
- Использует `stockTrnz` поле (которое теперь читает из "Trn" склада)

### 4. Исправление DAX запросов — склады
- **Капуа**: убраны запросы Trnz (stockTrnz и daySalesTrnz) — капуа = только Main + Zafn
- **Дагим**: изменены запросы с `"Trnz"` → `"Trn"` (реальный transit склад с 61 товаром)
- **Итоговая архитектура складов:**
  - קפוא, חלבי, דג יבש: Main + Zafn
  - דגים: Main + Zafn + Trn (מעבר)

### 5. Диагностика PBI данных (воскресенье)
- PBI данные от 20.05 (четверг) — PBI не рефрешит в выходные
- Пользователь подтвердил: воскресенье — рабочий день → нужно исправить расписание refresh в PBI Service
- GitHub Actions запускается исправно, просто получает стale данные от PBI

## Архитектура workflow (итог)
- **GitHub Actions** (автономно): `planogram-build.yml` — 06:00 + каждый час 16:00–23:00 Israel
- **Локальный watchdog** (backup): 07:30 ежедневно, `StartWhenAvailable=True`
- **CSV ברירת מחדל**: локально, меняется вручную → `run-workflow.bat` применяет

## Git commits
- `76544a1` — fix: default view, auto fit-to-screen, watchdog em-dash
- `84fc50d` — feat: TRN button dagim only
- `283a9de` — fix: dagim TRN Trn vs Trnz DAX
- `622519d` — fix: kapua remove Trnz queries

## Незакрытые задачи
- [ ] PBI расписание refresh — добавить воскресенье (делает пользователь в PBI Service)
- [ ] "שמור לענן" — кнопка сохранения планограммы для менеджера склада (4 секции)
- [ ] `project_halavi_dagim_layout_fix.md` — пустые серые строки halavi/dagim
- [ ] Analytics agent — нет Vault-заметки

## Related
[[mahsan-planogram]], [[session-2026-05-21]], [[agent-bug-agent]]
